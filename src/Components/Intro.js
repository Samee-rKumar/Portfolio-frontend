import React from 'react'
import { ReactTyped } from "react-typed";

export default function Intro() {
    return (
        <ReactTyped strings={["Web Developer"]} typeSpeed={40} backSpeed={50} loop />
    )
}
